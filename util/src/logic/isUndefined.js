/**
 * Check if the value is undefined.
 * @param valueReference - value to be checked.
 * @return {boolean}
 */
export const isUndefined = ( valueReference ) => {

    return typeof valueReference === 'undefined';

};