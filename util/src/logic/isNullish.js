/**
 * Check if value is null or undefined.
 * @param valueReference - value to be checked.
 * @return {boolean}
 */
export const isNullish = ( valueReference ) => {

    return valueReference == null;

};